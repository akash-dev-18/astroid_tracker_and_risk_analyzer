# Frontend Environment Variables
# Copy to .env.local for local development
# Set in Vercel dashboard for production

# Backend API URL (your Railway backend URL)
# For local dev: leave empty to use /api/v1 (proxy via vite config)
# For production: set to your Railway URL, e.g., https://cosmic-watch-backend.up.railway.app/api/v1
VITE_API_BASE_URL=
